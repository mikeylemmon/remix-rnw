import { Text } from "react-native";

export default function CompB() {
  return (
    <Text
      style={{
        color: "white",
        backgroundColor: "#063",
        padding: 15,
        borderRadius: 10,
      }}
    >
      Resolved to ~/app/components/CompB.tsx
    </Text>
  );
}
