import { Text } from "react-native";

export default function CompA() {
  return (
    <Text
      style={{
        color: "white",
        backgroundColor: "#055",
        padding: 15,
        borderRadius: 10,
      }}
    >
      Resolved to ~/app/components/CompA.web.tsx
    </Text>
  );
}
